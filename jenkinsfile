pipeline {
    agent any

    stages {
        stage('Install Apache') {
            steps {
                    sh '''
                    apt install -y apache2
                    '''
                
            }
        }
    }     
}

