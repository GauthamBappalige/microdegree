pipeline {
    agent any

    stages {
        stage('Install Apache') {
            steps {
                    sh '''
                    sudo apt install -y apache2
                    '''
                
            }
        }
    }     
}

